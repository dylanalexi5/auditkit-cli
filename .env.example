# Copiá este archivo como .env y pegá tu clave real ahí.
# .env está en .gitignore y nunca se commitea; este .env.example sí, y por eso
# no lleva ninguna credencial de verdad.

# Credencial del verificador semántico (Groq).
# Se obtiene en https://console.groq.com/keys
GROQ_API_KEY=tu_clave_aqui
